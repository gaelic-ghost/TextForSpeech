# TextForSpeech v0.18.7

## What changed

- changed the balanced and compact style policy so `::` stays silent while still separating namespaced code tokens into speakable words
- kept explicit style narration for `::` as `double colon` for audio-first code reading
- extended markdown inline-code and source-lane coverage for double-colon behavior
- updated maintainer notes to record that double-colon narration belongs in style policy rather than the semantic core

## Breaking changes

- none

## Migration notes

- no caller-side API changes are required for this release

## Verification performed

- `swift build`
- `swift test`
